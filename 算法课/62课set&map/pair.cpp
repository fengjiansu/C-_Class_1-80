#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pii;
#define F first
#define S second
#define MP make_pair
#define PB push_back
#define REP(i,a,b) for(int i=a;i<b;i++)

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    pair<string, vector<int>> p;
    auto p1 = make_pair("hello", vector<int>{1, 2, 3});
    cout << p1.first << " " << p1.second[0] << " " << p1.second[1] << " " << p1.second[2] << endl;
    return 0;
}
